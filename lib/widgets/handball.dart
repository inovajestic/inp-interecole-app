import 'package:flutter/material.dart';

class Handball extends StatelessWidget {

  Handball({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: CircularProgressIndicator(),
    );
  }
}